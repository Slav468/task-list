import './Footer.scss';

function Footer() {
	return (
		<footer className='footer'>
			<p>
				Technologies and React concepts used: React, JSX, props, useState,
				component composition, conditional rendering, array methods, event
				handling.
			</p>
		</footer>
	);
}

export default Footer;
